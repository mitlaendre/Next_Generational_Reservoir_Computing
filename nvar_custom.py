import itertools as it
from scipy.special import comb
import numpy as np
class NVAR():

    def __init__(self, delay: int, order: int):
        self.delay = delay
        self.order = order

    def fit(self,x_train, y_train, warmup=100):
        return

    def run(self,x_test):
        return
